// Should contain audio capture/recording component, local state to store captured audio.
//receive a function which dispatches an action from the component where it is being used;
/*in this case the commentEditor 
It should then fire that function when its send button is clicked; passing on its state as aurgument
like so; <button title="Done" onpress={()=>createAudioComment(audioComment)}
*/
