import React from "react";
import { View, Text } from "react-native";
import { styles } from "../../common/style/styles";

const ShowUserActiveSubscription = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}> YOUR ACTIVE SUBSCRIPTION </Text>
    </View>
  );
};

export default ShowUserActiveSubscription;
