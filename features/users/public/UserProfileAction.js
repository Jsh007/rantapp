/*
Define the functions which should be fired when the action buttons (stalk and chat) on the userprofile screen
are pressed. Then import styled button passing them necessary style options ( color, icon name, width, height)
and the function to fire when pressed. NOTE: one function per button

This component should be wrapped with the showProfileInfo so It could be displayed in place of its props.children
*/
